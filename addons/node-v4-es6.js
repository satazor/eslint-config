'use strict';

module.exports = {
    'rules': {
        // suggest using the spread operator instead of .apply()
        // comment: node v4 doesn't support spread
        'prefer-spread': 0,
    },
};
